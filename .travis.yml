language: node_js
node_js:

- "stable"
cache:
  directories:
    - "node_modules"

install: npm install
 services:
   - postgresql
env:
  - NODE_ENV=test

git: 
  depth: 50
  quiet: true

before_script:
  - psql -c 'drop database if exists db_test;' -U postgres
   -psql -c 'create database db_test;' -U postgres
#   - cat ./server/model/db/test.sql | psql -h localhost -U postgres db_test
   - psql -c "CREATE USER automart WITH PASSWORD '1NIGeria';" -U postgres
script: npm run test
after_success: npm run coverage
